<div class="pb-1">
  <h1 class="display-3 text-muted">Edit Litter</h1>
</div>

<%= render 'form', locals: {litter: @litter} %>

<div>
  <%= link_to "Cancel", litter_path(@litter), class: 'btn btn-outline-secondary' %>
</div>
